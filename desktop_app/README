## Setup
1. Create a virtual environment: `python -m venv .venv`
2. Activate it: `source venv/bin/activate` (Linux/Mac) or `.venv\Scripts\Activate.ps1` (Windows)
        If you get an error because this is disabled on: run Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
3. Install dependencies: `pip install -r requirements.txt`

## Reactivate before running python scripts and developing
