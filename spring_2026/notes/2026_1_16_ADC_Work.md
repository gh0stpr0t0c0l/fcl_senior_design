- GPIO35 (Pin 7) 
- ADC1
- At `ADC_ATTEN_DB_11` maximum voltage is limited by VDD_A (3.3V/2450 mV)

- `ADC_CHANNEL_7` → correct for GPIO35
- `ADC_ATTEN_DB_12`
- `ADC_BITWIDTH_12` → reasonable default

- [ ] Comments/headers 
- [x] Account for the fact that the ADC unit may not always provide a measurement [see](https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/peripherals/adc_oneshot.html#_CPPv4N22adc_oneshot_chan_cfg_t5attenE)
- [ ] Document the fact that the ADC unit is claimed here
- [ ] Make it not overflow

Oddly enough, commenting out line 50 (`ESP_LOGW(TAG, "%.3f,%2f,%2f\n", now_time / 1e6, pitch, roll);`) in `mpu_mgr.c` and line 52 (`ESP_LOGI(TAG, "Distance Left %d mm | Distance Right %d mm", ranges[1], ranges[0]);`) in `tof_mgr.c` causes the yellow light blink script to start working. This at git commit `14b5e783c7be194e3257c14b9cbf3629cd011cc3`

Ok, so now I need to make it log and change the value for the voltage divider scaling factor when I solder the resistor on.